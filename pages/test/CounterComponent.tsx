// components/CounterComponent.tsx

// example to use another slice


/*
"use client";

import React from "react";
import { useAppSelector, useAppDispatch } from "../../src/redux/store"; // Update the path

import { increment, decrement } from "../../src/redux/features/counter-slice"; // Update the path

const CounterComponent: React.FC = () => {
    const count = useAppSelector((state) => state.counterReducer.value);
    const dispatch = useAppDispatch();

    return (
        <div>
            <h2>Counter Component</h2>
            <p>Count: {count}</p>
            <button onClick={() => dispatch(increment())}>Increment</button>
            <button onClick={() => dispatch(decrement())}>Decrement</button>
        </div>
    );
};

export default CounterComponent;

 */
